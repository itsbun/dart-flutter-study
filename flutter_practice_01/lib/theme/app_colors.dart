import 'package:flutter/material.dart';

abstract final class AppColors {
  // Text
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF8C8C87);
  static const Color textTertiary = Color(0xFF4D4D4D);
  static const Color textQuaternary = Color(0xFFFFFFFF);

  // Background
  static const Color backgroundPrimary = Color(0xFFFFFFFF);

  // Border
  static const Color borderPrimary = Color(0xFFDEDED9);

  // Actions / Buttons
  static const Color buttonPrimary = Color(0xFF175291);

  // Status / Checkbox
  static const Color uncheck = Color(0xFFD9D9D4);
  static const Color checked = Color(0xFF3D8F59);
}
